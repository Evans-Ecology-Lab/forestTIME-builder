% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fia_design.R
\name{fia_design}
\alias{fia_design}
\title{Assign interpolated data plots to estimation units and strata}
\usage{
fia_design(data_annualized, db)
}
\arguments{
\item{data_annualized}{Annualized data produced by \code{\link[=fia_annualize]{fia_annualize()}}.}

\item{db}{The list of tables produced by \code{\link[=fia_load]{fia_load()}}.}
}
\description{
This adds columns from various \verb{POP_*} tables to annualized data so that it
can be used for stratified estimation (see, for example, \href{https://doserlab.com/files/rfia/articles/fiademystified#with-sampling-errors}{FIA Demystified}).
Each plot in each year is matched with an EVALID and it's associated data as
such:
\enumerate{
\item The EVALID must have both the \code{"EXPVOL"} and \code{"EXPCURR"} \code{EVAL_TYP}.
\item The \code{YEAR} must be between \code{START_INVYR} and \code{END_INVYR}.
\item The first matching EVALID is used—i.e. if the \code{YEAR} is 2002 and there are
EVALIDs for 2000--2003, 2001--2004, and 2002--2005, the one from
2000--2003 will be matched.
}
}
\details{
EVALID-associated data added by this function \strong{may be in conflict with the
results of interpolation by \code{fia_annualize()}!} When using this function to
do stratified estimation, use the \code{PLOT_STATUS_CD} as part of the domain
indicator to correctly exclude any non-sampled plots with no tree data!
}
\examples{
\dontrun{
# Load example data included in package
db <- fia_load("RI", dir = system.file("exdata", package = "forestTIME"))

# Annualize data
data_annualized <- db |> fia_tidy() |>
   fia_annualize(use_mortyr = FALSE)

# Assign plots to strata, estimation units, and EVLIDs
data_stratified <- fia_design(data_annualized, db)
}
}
\seealso{
\code{\link[=fia_eval_info]{fia_eval_info()}} to see all possible EVALIDs associated with plots.
}
