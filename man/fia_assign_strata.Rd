% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fia_assign_strata.R
\name{fia_assign_strata}
\alias{fia_assign_strata}
\title{Assign interpolated data plots to estimation units and strata}
\usage{
fia_assign_strata(data_annualized, db)
}
\arguments{
\item{data_annualized}{Annualized data produced by \code{\link[=fia_annualize]{fia_annualize()}}.}

\item{db}{The list of tables produced by \code{\link[=fia_load]{fia_load()}}.}
}
\description{
This adds columns from various \verb{POP_*} tables to annualized data so that it
can be used for stratified estimation (see, for example, \href{https://doserlab.com/files/rfia/articles/fiademystified#with-sampling-errors}{FIA Demystified}).
Each plot in each year is matched with an EVALID and it's associated data as such:
\enumerate{
\item The EVALID must have both the \code{"EXPVOL"} and \code{"EXPCURR"} \code{EVAL_TYP}.
\item The EVALID's \code{END_INVYR} must match the \code{YEAR} in the annualized data for
that plot.
\item When there are gaps (e.g. because a plot was not sampled and not belong to
an EVALID with \code{"EXPVOL"} or \code{"EXPCURR"}) the EVALIDs are filled down,
then up.
}
}
\details{
This means that the EVALID-associated data added by this function \strong{may be in
conflict with the results of interpolation by \code{fia_annualize()}!} When using
this function to do stratified estimation, use the \code{PLOT_STATUS_CD} as part
of the domain indicator to correctly exclude any non-sampled plots with no
tree data!
}
\examples{
\dontrun{

db <- fia_load("RI", dir = system.file("exdata", package = "forestTIME"))
data_annualized <- db |> fia_tidy() |>
   fia_annualize(use_mortyr = FALSE)
data_stratified <- fia_assign_strata(data_annualized, db)
}
}
