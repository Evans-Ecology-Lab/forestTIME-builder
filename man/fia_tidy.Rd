% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fia_tidy.R
\name{fia_tidy}
\alias{fia_tidy}
\title{Read in and join all required tables}
\usage{
fia_tidy(db)
}
\arguments{
\item{db}{a list of tables produced by \code{\link[=fia_load]{fia_load()}}}
}
\value{
a tibble
}
\description{
Reads in all the tables needed for carbon estimation and population scaling
and joins them into a single table. Then, some additional data cleaning steps
are performed.
\enumerate{
\item Creates unique tree and plot identifiers (\code{tree_ID} and \code{plot_ID},
respectively).
\item Fills in missing values for \code{ACTUALHT} with values from \code{HT} to prepare
for interpolation.
\item Overwrites \code{SPCD} with whatever the last value of \code{SPCD} is for each tree
(to handle trees that change \code{SPCD}).
\item Fills a tree's \code{MORTYR} column so every row contains the recorded
mortality year.
\item Adds \code{EVALID}-related columns. \code{EVALID}s are merged such that each plot
in each \code{INVYR} is associated with the \code{EVALID} of each type (last two
digits) that has the greatest end year.  Then, logical indicator columns
\code{EVAL_TYPE_EXPVOL} and \code{EVAL_TYPE_EXPCURR} are added along with other
columns from various \verb{POP_*} tables suffixed with \verb{_EXPVOL} and
\verb{_EXPCURR}.
}
}
\seealso{
\code{\link[=fia_add_composite_ids]{fia_add_composite_ids()}}
}
